'use strict';

/**
 * 优化模块管理器
 *
 * @author 老雷<leizongmin@gmail.com>
 */
 
/**
 * 优化模块管理器
 *
 * @param {Segment} 分词接口
 */ 
var Optimizer = module.exports = function (segment) {
  this.segment = segment;
};

/**
 * 对一段文本进行分词
 *
 * @param {array} words 单词数组
 * @param {array} modules 分词模块数组
 * @return {array}
 */
Optimizer.prototype.doOptimize = function (words, modules) {
  // 按顺序分别调用各个module来进行分词 ： 各个module仅对没有识别类型的单词进行分词
  modules.forEach(function (module) {
    words = module.doOptimize(words);
  });
  // 地名+机构名是否需要拆分  PorYoung
    for(var i = 0;i < words.length;i++){
      if(words[i].p == 64 && words[i+1].p == 32){
        words[i].w += words[i+1].w;
        words[i].p = words[i+1].p;
        for(var j = i+1;j < words.length;j++){
          words[j] = words[j+1];
        }
        words.length -= 1;
      }
    }
  return words;
};
